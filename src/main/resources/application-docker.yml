server:
  port: ${SERVER_PORT:8081}

spring:
  datasource:
    url: ${IDENTITY_DB_URL:${DB_URL:jdbc:mysql://mysql-identity:3306/banquito_identity_access_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Guayaquil}}
    username: ${IDENTITY_DB_USER:${DB_USERNAME:root}}
    password: ${IDENTITY_DB_PASSWORD:${DB_PASSWORD:${MYSQL_ROOT_PASSWORD:BanQuitoRoot2026!}}}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    baseline-version: ${FLYWAY_BASELINE_VERSION:1}

banquito:
  security:
    jwt:
      issuer: ${JWT_ISSUER:banquito-identity-access}
      secret: ${JWT_SECRET:BanQuitoIdentityJwtSecretKeyForDevelopmentOnlyChangeMe2026}
      access-token-minutes: ${ACCESS_TOKEN_MINUTES:15}
      refresh-token-days: ${REFRESH_TOKEN_DAYS:7}
  demo:
    enabled: ${DEMO_DATA_ENABLED:true}
    password: ${DEMO_PASSWORD:password}
